version: '3.4'
  
networks:
  web:
    external:
      name: web
services:
  ui:
    labels:
      - 'traefik.backend=go-ui'
      - 'traefik.port=8044'
      - 'traefik.frontend.rule=Host:lifereferencemanual.net'
    networks:
      web:
    image: wayimp/go-ui
    ports:
      - 8044:8044